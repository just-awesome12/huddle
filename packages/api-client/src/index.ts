// Supabase client wrapper for Huddle.
//
// Phase 2 will add: `createBrowserClient`, `createServerClient`,
// `createNativeClient`, plus typed query hooks. Phase 0 leaves this
// module empty so the package is wired into the workspace graph but
// ships no logic yet.

export {};
